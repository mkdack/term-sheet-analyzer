[build]
  publish = "."
  command = "npm install"

[build.environment]
  NODE_VERSION = "20"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[functions."extract"]
  timeout = 60

[functions."extract-a"]
  timeout = 26

[functions."extract-b"]
  timeout = 26

[functions."extract-b1"]
  timeout = 26

[functions."extract-b2"]
  timeout = 26

[functions."score"]
  timeout = 60

[functions."extract-custom"]
  timeout = 26

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[[headers]]
  for = "/*"
  [headers.values]
    Content-Security-Policy = "default-src 'self'; script-src 'self' 'unsafe-inline' https://cdnjs.cloudflare.com https://fonts.googleapis.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; connect-src 'self' https://api.anthropic.com /.netlify/functions/*;"
    Cache-Control = "no-cache, no-store, must-revalidate"
